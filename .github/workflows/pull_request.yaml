name: Pull request
on: pull_request
jobs:
  changelog_ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/changelog_ci
        with:
          committer_username: ilic90
          committer_email: dusan.ilic.90.nis@gmail.com
          release_version: v0.0.1